# Docker образ симулятора Sverk

> **Для кого это руководство?**  
> Это инструкция для пользователей, работающих с автономными дронами на базе **ROS 2 Humble**, **PX4**, **Gazebo** и **MicroXRCE-DDS-Agent** и желающих запускать всё это в изолированной, но удобной Docker-среде с GUI-доступом через браузер.

## Характеристики системы

- **ROS 2** — Humble  
- **PX4** — v1.16.0  
- **Micro-XRCE-DDS-Agent** — v2.4.3  
- **QGroundControl** — встроено  
- **ОС** — Ubuntu 22.04  

---

## Основные команды

### 🔧 Сборка контейнера из исходников

```bash
docker build -t sverk-docker:v1.1 .
```

> **Что делает эта команда?**  
> Собирает Docker-образ из `Dockerfile`, находящегося в текущей директории, и присваивает ему имя `sverk-docker` с тегом `v1.1`.

---
### 🔧 Скачивание контейнера из Docker Hub

```bash
docker pull ilyawwww/sverk-docker:v1.3
```

> **Что делает эта команда?**  
> Загружает готовый Docker-образ `sverk-docker` версии `v1.3` из репозитория `ilyawwww` на Docker Hub. Этот образ содержит предустановленную среду симулятора Sverk со всеми зависимостями.

> **Преимущества скачивания из Docker Hub:**
> - Не требуется сборка образа на локальной машине
> - Экономия времени (не нужно компилировать компоненты)
> - Гарантированно рабочая версия
> - Автоматическое получение обновлений
---

### ▶️ Запуск контейнера (без общей папки)

```bash
docker run -it --rm -d -p 6080:6080 -p 5900:5900 --security-opt seccomp=unconfined --shm-size=2g --name sverk-docker-container sverk-docker:v1.0
```

> **Пояснение флагов**:
> - `-it` — интерактивный режим с TTY (позволяет взаимодействовать с контейнером, если понадобится).
> - `--rm` — автоматически удалить контейнер после остановки.
> - `-d` — запуск в фоне (detached mode).
> - `-p 6080:6080 -p 5900:5900` — проброс портов для VNC-доступа к GUI (через браузер или VNC-клиент).
> - `--security-opt seccomp=unconfined` — отключает профиль безопасности seccomp, что **требуется для Gazebo и PX4 SITL** . Без этого могут возникнуть ошибки при запуске симуляции.
> - `--shm-size=2g` — увеличивает размер shared memory до 2 ГБ (по умолчанию — 64 МБ), что критично для Gazebo и ROS-нод, активно использующих IPC .
> - `--name sverk-docker-container` — задаёт имя контейнеру для удобства управления.

---

### 💾 Клонирование рабочей директории из контейнера

```bash
docker cp sverk-docker-container:/home/user/sverk_ws .
```

> **Важно!**  
> Перед первым запуском с общей папкой (через `-v`) **обязательно** выполните эту команду, чтобы скопировать свежесозданный `sverk_ws` (рабочее пространство ROS 2) из контейнера на хост.  
> Если этого не сделать, при монтировании пустая локальная папка `sverk_ws` **перекроет** содержимое внутри контейнера, и вы потеряете все собранные пакеты.  
> ⚠️ Требует прав администратора, если вы на Windows и используете Docker Desktop без WSL2-интеграции.


---

### 📁 Запуск контейнера с общей директорией

```bash
docker run -it --rm -d -p 6080:6080 -p 5900:5900 --security-opt seccomp=unconfined --shm-size=2g -v ${PWD}/sverk_ws:/home/user/sverk_ws --name sverk-docker-container sverk-docker:v1.0
```

> **Что делает `-v ${PWD}/sverk_ws:/home/user/sverk_ws`?**  
> Монтирует локальную папку `sverk_ws` (в текущем каталоге хоста) внутрь контейнера по пути `/home/user/sverk_ws`. Это позволяет **сохранять изменения** (например, исходный код, результаты сборки) даже после остановки контейнера.

> **❗ Перед первым запуском с `-v` обязательно скопируйте `sverk_ws` из контейнера** (см. предыдущий шаг), иначе вы монтируете **пустую папку**, и всё содержимое внутри контейнера пропадёт!

---

### ⏹️ Управление жизненным циклом контейнера

Остановка и удаление контейнера (после `--rm` он удалится автоматически):

```bash
docker stop sverk-docker-container
```

Временная пауза выполнения (полезно для экономии ресурсов):

```bash
docker pause sverk-docker-container
```

Возобновление после паузы:

```bash
docker unpause sverk-docker-container
```

---

### 🔐 Доступ к GUI

- **Логин/пароль** внутри контейнера: `user:password`
- **Доступ через браузер**: [http://localhost:6080/vnc.html](http://localhost:6080/vnc.html)

> GUI работает через **noVNC** — веб-интерфейс для VNC. Порт `6080` — HTTP-доступ к noVNC, `5900` — прямой VNC (для клиентов типа RealVNC).

---

### 🚁 Запуск симулятора PX4 + Gazebo

Запуск дрона без периферии
```bash
make px4_sitl gz_x500
```

С камерой (направленной вниз):

```bash
make px4_sitl gz_x500_mono_cam_down
```

С камерой и ArUco-маркёрами:

```bash
make px4_sitl gz_x500_mono_cam_down_aruco
```

> Эти команды запускают PX4 SITL (Software In The Loop) и Gazebo с заданной моделью дрона.

---

### 📡 Запуск MicroXRCEAgent

```bash
MicroXRCEAgent udp4 -p 8888
```

> Этот агент устанавливает связь между ROS 2 и PX4 по UDP. Порт `8888` — стандартный для симуляции.

---

### 📊 Проверка работы датчиков

```bash
ros2 launch px4_ros_com sensor_combined_listener.launch.py
```

> Выводит данные с IMU, магнитометра и других датчиков в терминал.

---

### 🎮 Управление дроном (offboard mode)

```bash
ros2 run px4_ros_com offboard_control
```

> Запускает пример управления дроном в offboard-режиме (через ROS 2 → PX4).

---

### 🛰️ Запуск QGroundControl
```bash
./home/user/edit_rcS.bash
```

> Эта команда настраивает сетевые правила, позволяя подключиться к QGroundControl **с хоста**.

> **Альтернативы**:
> - Запустить `QGroundControl` на рабочем столе в Docker — тогда QGC будет работать **внутри контейнера** (через VNC).
> - Запустить `Remote QgroundControl` — тогда QGC будет работать на **вашем хосте**, подключаясь к симуляции в контейнере.

---

## 📷 Работа с камерой в симуляторе

### Шаг 1: Запустите симуляцию с камерой

```bash
make px4_sitl gz_x500_mono_cam_down_aruco
```

### Шаг 2: Пробросьте изображение из Gazebo в ROS 2

```bash
ros2 run ros_gz_bridge parameter_bridge /world/aruco/model/x500_mono_cam_down_0/link/camera_link/sensor/imager/image@sensor_msgs/msg/Image@gz.msgs.Image
```

> **❗ Важно**: путь топика может отличаться! Уточните актуальный топик:

```bash
gz topic -l | grep image
```

> Команда `gz topic -l` выводит список всех топиков в Gazebo. Фильтрация через `grep image` показывает только изображения.

### Шаг 3: Просмотр изображения

Через **RViz2**:

```bash
rviz2
```

Или через **rqt_image_view** (проще и легче):

```bash
ros2 run rqt_image_view rqt_image_view
```

> В `rqt_image_view` выберите нужный топик (например, `/world/aruco/.../image`) из выпадающего списка.

---

## 🛠️ Отладка и оптимизация

### Дополнительные флаги для повышения производительности

```bash
-m 8g --memory-swap=8g --cpus=8.0 --gpus all
```

> **Пояснение**:
> - `-m 8g` — ограничивает контейнер 8 ГБ оперативной памяти.
> - `--memory-swap=8g` — **общий лимит** на RAM + swap. Если RAM = 8 ГБ, то swap = 0. Если указать `--memory-swap=16g`, то RAM=8ГБ + swap=8ГБ .
> - `--cpus=8.0` — выделяет 8 ядра CPU.
> - `--gpus all` — даёт контейнеру доступ ко **всем GPU** на хосте .  
>   ⚠️ Требует установки **NVIDIA Container Toolkit** на хосте .

> **Пример полной команды с GPU и памятью**:

```bash
docker run -it --rm -d \
  -p 6080:6080 -p 5900:5900 \
  --security-opt seccomp=unconfined \
  --gpus all \
  --shm-size=2g \
  -m 8g --memory-swap=16g \
  --cpus=8.0 \
  -v ${PWD}/sverk_ws:/home/user/sverk_ws \
  --name sverk-docker-container \
  sverk-docker:v1.0
```

---

## 📦 Публикация образа в Docker Hub

Если вы хотите сохранить ваш образ в облаке (например, для совместной работы или развёртывания на другом компьютере), можно загрузить его в **Docker Hub**.

### 1. Присвойте образу имя, совместимое с Docker Hub

```bash
# Формат: docker tag <локальный_образ:тег> <имя_в_Docker_Hub/репозиторий:тег>
docker tag sverk-docker:v1.1 ilyawwww/sverk-docker:v1.3
```

### 2. Войдите в свой аккаунт Docker Hub

```bash
docker login
```

### 3. Загрузите образ в облако

```bash
docker push ilyawwww/sverk-docker:v1.3
```
---

## 💡 Советы

- **Не монтируйте пустую папку** — сначала скопируйте `sverk_ws` из чистого контейнера.
- **Для Windows**: убедитесь, что Docker Desktop настроен на использование **WSL2**, иначе монтирование и производительность будут проблемными.